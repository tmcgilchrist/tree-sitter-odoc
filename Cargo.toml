[package]
name = "tree-sitter-odoc"
description = "Odoc grammar for tree-sitter"
version = "0.2.0"
authors = ["Tim McGilchrist <timmcgil@gmail.com>"]
keywords = ["incremental", "parsing", "odoc", "ocaml"]
categories = ["parsing", "text-editors"]
repository = "https://github.com/tmcgilchrist/tree-sitter-odoc"
edition = "2021"
license = "MIT"
readme = "README.md"
autoexamples = false

build = "bindings/rust/build.rs"
include = ["LICENSE", "bindings/rust/*", "grammar.js", "queries/*", "src/*", "tree-sitter.json"]

[lib]
path = "bindings/rust/lib.rs"

[dependencies]
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1.1"

[dev-dependencies]
tree-sitter = "0.25"
